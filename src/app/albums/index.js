import React from 'react';

import { Layout } from '../shared/layout';
import { AlbumList } from './albumList';

export const Albums = () => {
  return (
    <Layout>
      <AlbumList />
    </Layout>
  );
};
